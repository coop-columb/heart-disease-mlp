{
    "# Instructions": "Copy this file to cloud_credentials.json and fill in your cloud storage credentials",
    "aws_access_key_id": "YOUR_AWS_ACCESS_KEY_ID",
    "aws_secret_access_key": "YOUR_AWS_SECRET_ACCESS_KEY",
    "s3_bucket": "your-s3-bucket-name",

    "azure_connection_string": "YOUR_AZURE_CONNECTION_STRING",
    "azure_container": "your-azure-container-name",

    "gcp_bucket": "your-gcp-bucket-name",
    "# Note": "GCP authentication uses Application Default Credentials (ADC). Run 'gcloud auth application-default login' to set up."
}
